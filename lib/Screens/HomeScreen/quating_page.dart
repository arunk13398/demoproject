import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class QuatingPage extends StatefulWidget {
  const QuatingPage({Key? key}) : super(key: key);

  @override
  State<QuatingPage> createState() => _QuatingPageState();
}

class _QuatingPageState extends State<QuatingPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child:Column(
          children: [
            Container(),
            Container(),
          ],
        )
      ),
    );
  }
}
